{% extends "controles/base.html"%}

{% block title %} Teste {% endblock %}

{% block corpo %} 
	<p>{{ sala.description }}</p>
	{% if weather  %}
		<p>Umidade: {{ weather.umidade }}</p>
		<p>Temperatura: {{ weather.temperatura }}</p>	
	{% endif%}
	{% if controles %}
		{% for controle in controles %}
			<a href="/controle/{{ controle.id}}/{{ sala.id }}" class="btn btn-default">{{ controle.description }}</a>
		{% endfor %}
	{% else %}
		<p>Não encontrei nenhum</p>
	{% endif %}
{% endblock %}  
