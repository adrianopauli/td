{% extends "controles/base.html"%}

{% block title %} Controles {% endblock %}

{% block content %} 
	<p>Controles para a sala {{ sala.description }}</p>
	{% if weather  %}
		<ul class="list-goup">
			<li class="list-group-item">
			Umidade
				<span class="badge">{{ weather.umidade }} °</span>
			</li>	
			<li class="list-group-item">
			Temperatura
				<span class="badge">{{ weather.temperatura }} °C</span>
			</li>
		</ul>
	{% endif%}
	{% if controles %}
		<ul>
			{% for controle in controles %}
			<li><a href="/controle/{{ controle.id}}/{{ sala.id }}">{{ controle.description }}</a></li>
			{% endfor %}
		</ul>
	{% else %}
		<p>Não encontrei nenhum</p>
	{% endif %}
{% endblock %}  
